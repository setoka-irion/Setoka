<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.practice.setoka.mapper.MessageMapper">


	<insert id="insertMessage" parameterType="com.practice.setoka.dto.MessageDto">
		INSERT INTO message (sender, receiver, title, content, item_Type, item_Value) VALUES (#{sender}, #{receiver}, #{title}, #{content}, #{item_Type}, #{item_Value})
	</insert>

	<select id="selectAllMessage" resultType="com.practice.setoka.dao.Message">
    	SELECT * FROM message
  	</select>
  	
  	<select id="selectToNum">
  		SELECT * FROM message WHERE num = #{num}
  	</select>
  	
	<select id="selectSenderMessage" resultType="com.practice.setoka.dao.Message" parameterType="String">
    	SELECT * FROM message WHERE sender = #{id} ORDER BY sendTime DESC
  	</select>
  	
	<select id="selectReceiverMessage" resultType="com.practice.setoka.dao.Message" parameterType="String">
    	SELECT * FROM message WHERE receiver = #{id} ORDER BY sendTime DESC
  	</select>
	
	<update id="updateMessageStatus">
		UPDATE message SET messageStatus = #{status} WHERE num = #{num}
	</update>
</mapper>